server:
pip install sounddevice

pip install soundfile

pip install numpy

pip install cryptography






client:

pip install sounddevice

pip install soundfile

pip install numpy

pip install cryptography

pip3 install customtkinter

pip install Pillow
